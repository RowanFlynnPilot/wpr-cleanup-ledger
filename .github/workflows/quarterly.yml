name: Quarterly bulk ingest

on:
  schedule:
    - cron: "0 12 5 3,6,9,12 *"   # 5th of Mar/Jun/Sep/Dec; bulk publishes ~the 1st
  workflow_dispatch:

permissions:
  contents: write

# Shares the nightly workflow's group: bulk ingest and map pull must not
# mutate the DB and push at the same time. One pending run per group —
# a newer queued run cancels an older queued one, so if this quarterly
# run ever shows as canceled, re-dispatch it manually.
concurrency:
  group: data-pipeline

jobs:
  ingest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      pushed: ${{ steps.commit.outputs.pushed }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install -r requirements.txt
      - run: python ingest/ingest_bulk.py
      - run: python build/build_json.py
      - name: Commit if changed
        id: commit
        run: |
          git config user.name "cleanup-ledger-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data public
          if git diff --cached --quiet; then
            echo "pushed=false" >> "$GITHUB_OUTPUT"
          else
            git commit -m "Quarterly bulk ingest"
            git push
            echo "pushed=true" >> "$GITHUB_OUTPUT"
          fi

  deploy:
    needs: ingest
    if: needs.ingest.outputs.pushed == 'true'
    uses: ./.github/workflows/deploy.yml
    permissions:
      contents: read
      pages: write
      id-token: write
